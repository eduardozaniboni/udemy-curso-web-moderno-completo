const calculosMatematicos = function (x, y) {
    console.log(`
    A soma é = ${x + y}
    A subtração é = ${x - y}
    A multiplicação é = ${x * y}
    A divisão é = ${x / y}`)
}

calculosMatematicos(20, 2)
